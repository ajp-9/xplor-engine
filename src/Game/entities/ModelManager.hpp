#pragma once
/*
enum model
{
	crate,
	chinese_box
};

struct ModelManager
{
	std::vector<xplor::Model> models;

	void loadModels()
	{
		models.push_back(xplor::Model("crate"));
		models.push_back(xplor::Model("chinese_box"));
	}
};
*/